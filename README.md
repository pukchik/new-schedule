# 📅 Расписание колледжа — eralas.ru

Веб-приложение для удобного просмотра расписания колледжа с умной логикой отображения актуальных занятий. Автоматически показывает следующую пару, если она ещё не прошла, или ближайший день с занятиями.

**🌐 Сайт проекта:** [eralas.ru](https://eralas.ru)

---

## 💻 Технологический стек

![Node.js](https://img.shields.io/badge/Node.js-18+-339933?style=for-the-badge&logo=node.js&logoColor=white)
![Express](https://img.shields.io/badge/Express-4.x-000000?style=for-the-badge&logo=express&logoColor=white)
![JavaScript](https://img.shields.io/badge/JavaScript-ES6+-F7DF1E?style=for-the-badge&logo=javascript&logoColor=black)
![HTML5](https://img.shields.io/badge/HTML5-E34F26?style=for-the-badge&logo=html5&logoColor=white)
![CSS3](https://img.shields.io/badge/CSS3-1572B6?style=for-the-badge&logo=css3&logoColor=white)
![Docker](https://img.shields.io/badge/Docker-2496ED?style=for-the-badge&logo=docker&logoColor=white)
![Caddy](https://img.shields.io/badge/Caddy-1F88C0?style=for-the-badge&logo=caddy&logoColor=white)

---

## ✨ Особенности

- **🧠 Умное отображение** — показывает только актуальную информацию: следующую пару или ближайший учебный день
- **⚡ Двухуровневое кэширование** ��� файловая система + оперативная память для быстрого доступа
- **👥 Поддержка групп и преподавателей** — расписание для студентов и преподавателей
- **📆 Экспорт в календарь** — генерация `.ics` файлов для импорта в любой календарь
- **🔄 Автообновление** — планировщик обновляет кэш каждые 20 минут
- **🛡️ Отказоустойчивость** — graceful degradation при сбоях парсинга
- **🐳 Docker-ready** — готовая контейнеризация с multi-stage build

---

## 🏗️ Архитектура

```
new-schedule/
├── back/                    # Backend (Node.js + Express)
│   ├── server.js           # Основной сервер
│   ├── calendar.js         # Генерация iCalendar
│   ├── teacher.js          # Парсер расписания преподавателей
│   ├── utils.js            # Утилиты
│   ├── sirinium/           # Локальный парсер (модифицированный)
│   └── static/             # Статические JSON данные
│       ├── groups.json     # Список групп
│       └── teachers.json   # Список преподавателей
├── front/                   # Frontend (Vanilla JS)
│   ├── index.html          # Главная страница
│   ├── style.css           # Стили
│   └── script.js           # Клиентская логика
├── deploy/                  # Конфигурация деплоя
│   ├── docker-compose.yml  # Docker Compose
│   └── Caddyfile           # Конфиг Caddy для reverse proxy
├── Dockerfile              # Multi-stage Docker build
└── package.json            # Зависимости проекта
```

**Backend:**
- Node.js 18+ — серверная платформа
- Express 4.x — веб-фреймворк
- CORS — кросс-доменные запросы
- Undici — современный HTTP-клиент с поддержкой proxy
- Sirinium (локальная версия) — парсер расписания

**Frontend:**
- HTML5 + CSS3
- Vanilla JavaScript (без фреймворков)

**Инфраструктура:**
- Docker + multi-stage build
- Caddy — reverse proxy и HTTPS
- File-based + In-Memory кэширование

---

## 🚀 Быстрый старт

### Локальная разработка

**Требования:** Node.js 18+

```bash
# Клонировать репозиторий
git clone https://github.com/pukchik/new-schedule.git
cd new-schedule

# Установить зависимости
npm install

# Запустить сервер
npm start

# Приложение доступно на http://localhost:3000
```

### 🐳 Запуск с Docker

```bash
# Сборка образа
docker build -t schedule-app .

# Запуск контейнера
docker run --rm -p 3000:3000 \
  -e BATCH_DELAY_MS=120000 \
  -e CACHE_FAILURE_BACKOFF_MS=2400000 \
  --name schedule schedule-app
```

### 🎯 Деплой с Docker Compose

```bash
cd deploy
docker-compose up -d
```

**Переменные окружения:**

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `BATCH_DELAY_MS` | Задержка между запросами при обновлении кэша (мс) | `120000` (2 мин) |
| `CACHE_FAILURE_BACKOFF_MS` | Дополнительная задержка при ошибке обновления (мс) | `2400000` (40 мин) |
| `HTTP_PROXY` / `HTTPS_PROXY` | Proxy для исходящих запросов | — |

---

## 📡 API

### Расписание группы

```http
GET /api/schedule?group=<ГРУППА>&week=<НЕДЕЛЯ>
```

**Параметры:**
- `group` *(обязательный)* — название группы
- `week` *(опциональный)* — номер недели:
  - `0` — текущая неделя (с кэшем) **[по умолчанию]**
  - `1` — следующая неделя (с кэшем)
  - другие значения — запрос без кэша

**Пример:**
```bash
curl "https://eralas.ru/api/schedule?group=К0709-24/2&week=0"
```

### Список групп

```http
GET /api/groups
```

**Ответ:** JSON-массив доступных групп

### Список преподавателей

```http
GET /api/teachers
```

**Ответ:** JSON-объект преподавателей с идентификаторами

### Расписание преподавателя

```http
GET /api/teacherschedule?id=<ID>&week=<НЕДЕЛЯ>
```

**Па��аметры:**
- `id` *(обязательный)* — идентификатор преподавателя
- `week` *(опциональный)* — номер недели (аналогично `/api/schedule`)

### Календарь группы (iCalendar)

```http
GET /calendar/group?group=<ГРУППА>
```

Возвращает `.ics` файл с расписанием группы на текущую и следующую недели.

### Календарь преподавателя (iCalendar)

```http
GET /calendar/teacher?id=<ID>
```

Возвращает `.ics` файл с расписанием преподавателя на текущую и следующую недели.

---

## 🗃️ Система кэширования

Приложение использует двухуровневую систему кэширования:

### 1️⃣ Файловое кэширование
- Расписание сох��аняется в `back/cache/groups/<группа>.json`
- Преподаватели — в `back/cache/teachers_cache.json`
- Персистентность между перезапусками

### 2️⃣ In-Memory кэш
- Быстрый доступ к часто запрашиваемым данным
- Автоматическое заполнение при старте из файлового кэша

### Планировщик обновлений
- Обновление каждые **20 минут** (текущая + следующая недели)
- При ошибке — дополнительная задержка **40 минут** (backoff)
- Graceful degradation: при сбое используется старый кэш

### Защита от перегрузки
- Батчевая обработка с задержкой между запросами (`BATCH_DELAY_MS`)
- Остановка обновления при первой ошибке в цикле
- Раздельное обновление групп и преподавателей

---

## 🎯 Логика отображения

Фронтенд следует умной логике определения актуального расписания:

1. ✅ **Если сегодня есть предстоящие пары** → показывается следующая ближайшая пара
2. ⏭️ **Если на сегодня пары закончились** → показывается расписание на следующий учебный день
3. 📅 **Если сегодня нет занятий** → показывается ближайший день с парами

Пользователь всегда видит только релевантную информацию без лишних д��нных.

---

## 🛠️ Разработка

### Структура backend модулей

- **`server.js`** — главный сервер, маршруты, кэширование
- **`calendar.js`** — генерация iCalendar файлов
- **`teacher.js`** — класс для парсинга расписания преподавателей
- **`utils.js`** — вспомогательные функции
- **`sirinium/`** — локально модифицированная версия парсера Sirius

### Добавление новой группы/преподавателя

1. Обновите `back/static/groups.json` или `back/static/teachers.json`
2. Перезапустите сервер или дождитесь следующего обновления кэша

---

### Нашли проблему?

[Создайте issue](../../issues/new) с подробным описанием бага или предложением по улучшению.

---

## 🙏 Благодарности

- **[sirinium](https://github.com/perovxp/sirinium)** — за базовый парсер расписания
- Всем контрибьюторам проекта
